<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:vm="clr-namespace:TCMobile.ViewModels"
             x:Class="TCMobile.Views.Login"
             BackgroundColor="#337ab7"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:LoginViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <StyleSheetExtension Source="../style.css"></StyleSheetExtension>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Spacing="10" VerticalOptions="StartAndExpand">            
               <Entry x:Name="Email" Text="{Binding Email}"
                               Placeholder="Email"   
                               HeightRequest="45" 
                               Keyboard="Email"/>
                <Entry x:Name="Password" Text="{Binding Password}" Placeholder="Password"   
                               HeightRequest="45" 
                               IsPassword="True"/>
            <Button Text="Login" VerticalOptions="End" Clicked="OnLoginButtonClicked"/>
            <Label x:Name="Error" HeightRequest="45"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>